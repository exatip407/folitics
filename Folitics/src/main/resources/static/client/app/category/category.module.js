(function(angular) {

	'use strict';

	angular.module('governProject.category', []).config(Configure);

	Configure.$inject = [ '$routeProvider' ];

	function Configure($locationProvider, $routeProvider) {

	}

})(window.angular);