(function(angular) {
	'use strict';

	angular.module('governProject.contest', []).config(Configure);

	Configure.$inject = [ '$routeProvider' ];

	function Configure($locationProvider, $routeProvider) {
	}

})(window.angular);