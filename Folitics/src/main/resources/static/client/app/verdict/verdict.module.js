(function(angular) {
	'use strict';

	angular.module('governProject.verdict', []).config(Configure);

	Configure.$inject = [ '$routeProvider' ];

	function Configure($locationProvider, $routeProvider) {
	}

})(window.angular);