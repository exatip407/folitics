
(function (angular) {
    'use strict';

    angular.module('governProject.gaHeader', []).config(Configure);

    Configure.$inject = ['$routeProvider'];

    function Configure($locationProvider, $routeProvider) {
         
    }

})(window.angular);